{
  "name": "vechile-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "npm run start && cd maps && npm install",
    "start": "npm install && node server.js"
    "build": "cd maps && npm run build && cd maps && npm start",
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "underscore": "^1.13.7"
  }
}
